// uno.config.ts
import { defineConfig } from 'unocss'

export default defineConfig({
	theme: {
		fontFamily: {
			sans: '"Century Gothic", sans-serif',
			serif: 'Alegreya, serif',
			display: '"Catchy Mager", sans-serif',
		},
	},
})
